describe("Comment Service", () => {
  beforeAll(() => {
    // initializations
    // e.g DAO initialization
  });
  beforeEach(() => {
    // reset initializations
    // e.g in-memory db clear
  });

  describe("add comment", () => {
    it("should insert comment in the database", () => {
      // some testing logic
    });
  });

  describe("edit comment", () => {
    it("should modify comment in the database", () => {
      // some testing logic
    });
  });
});
